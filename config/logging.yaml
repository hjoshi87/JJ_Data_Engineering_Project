# Logging Settings
logging:
  level: INFO
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  
# File Logging
file:
  enabled: true
  path: ./logs/pipeline.log
  max_size: 10485760  # 10MB
  backup_count: 5
  
# Console Logging
console:
  enabled: true
  level: INFO